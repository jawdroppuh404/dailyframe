# Local dev: use any Postgres URL (Neon, Supabase, Docker, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Vercel Blob is configured in Vercel dashboard; env vars are automatically added.
# No extra env vars required here for the server-side put() approach.
